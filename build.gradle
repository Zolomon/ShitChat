apply plugin: 'java'
apply plugin: 'application'

sourceCompatibility = '1.7'
version = '1.0'

mainClassName = 'com.zolomon.eda095.project.Main'

jar {
  manifest {
    attributes 'Implementation-Title': 'TowerDefen(c|s)e',
            'Implementation-Version': version
  }
}
